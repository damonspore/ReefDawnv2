{%- comment -%}
  Avada SEO obfuscated script blocker and LightJsLoader removed.
  The original code added ~28KB of inline JS per page, monkey-patched document.createElement,
  hijacked content_for_header, and installed MutationObservers on the entire DOM.
  All theme JS files already use defer — no additional script blocking needed.
{%- endcomment -%}

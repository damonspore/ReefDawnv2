{%- comment -%}
  Avada SEO 404 redirect removed — was hiding the page body and auto-redirecting to homepage
  after 500ms, preventing users from seeing the 404 page. Use Shopify's native 404 template instead.
{%- endcomment -%}
